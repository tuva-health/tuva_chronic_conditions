version: 2

sources:
  - name: core
    database: "{{ var('tuva_chronic_coditions_input_database','tuva') }}"
    schema:  "{{ var('tuva_chronic_coditions_input_schema','core') }}"
    tables:
      - name: condition